[project]
name = "data-transformation-agent"
version = "0.1.0"
description = "Autonomous document-to-CSV transformation agent with self-correcting validation."
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
